<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.videogame">

    <application
        android:allowBackup="true"
        android:icon="@drawable/images"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".favorite" />
        <activity android:name=".Plane" />
        <activity android:name=".SplashPlane" />
        <activity android:name=".NewLevel" />
        <activity android:name=".Fish" />
        <activity android:name=".GameOver" />
        <activity android:name=".SplashFish" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".profile" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>